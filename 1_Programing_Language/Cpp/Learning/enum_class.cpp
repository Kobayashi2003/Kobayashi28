// enum class

#include <iostream>

enum class Number { One, Two, Three };
enum class Color { Red, Green, Blue };

int main() {
    Number num = Number::One;
    Color col = Color::Red;
    return 0;
}