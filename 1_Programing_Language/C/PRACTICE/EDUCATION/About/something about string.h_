#include<string.h>

puts()
gets()

strcat(,)

strcpy(字符数组,字符串)
strncpy(字符数组,字符串,n)

strcmp(字符串1,字符串2)

strlen(字符数组||字符串常量)
strnlene(字符数组||字符串常量,n)

strlwr(字符串)
strupr(字符串)

memset(字符数组/指针变量,要赋予的值,数组的长度)